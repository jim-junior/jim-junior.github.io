import { Divider, Typography } from "@mui/joy";

const Header = () => {
  return (
    <div>
      <Typography level="h1">Its showtime🍿</Typography>
      <Typography level="body-md">
        Works of film that had an impact on my and my world view, plus the nice
        ones too
      </Typography>
      <Divider />
    </div>
  );
};

export default Header;
