import React from "react";

export const metadata = {
  title: "An Introduction to Supervised Learning Models",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
